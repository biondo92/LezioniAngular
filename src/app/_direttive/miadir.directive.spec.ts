import { MiadirDirective } from './miadir.directive';

describe('MiadirDirective', () => {
  it('should create an instance', () => {
    const directive = new MiadirDirective();
    expect(directive).toBeTruthy();
  });
});
